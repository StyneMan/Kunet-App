import 'package:flutter/material.dart';

class Dashbboard extends StatelessWidget {
  const Dashbboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
